// ***********************************************************
// This support file is loaded before all component tests
// Use this for component-specific setup
// ***********************************************************

// Import Cypress commands
import './commands';

// Import global styles for component testing
import '../../src/styles.scss';

// Mount command for Angular components
// Note: Requires @cypress/angular or manual setup with Angular's TestBed
// For now, this is a placeholder. To use component testing:
// 1. Install: npm install -D @cypress/angular
// 2. Configure in cypress.config.ts
// 3. Use cy.mount() in component tests

// Example when @cypress/angular is installed:
// import { mount } from 'cypress/angular';
// Cypress.Commands.add('mount', mount);

// Placeholder to prevent empty file
export {};
